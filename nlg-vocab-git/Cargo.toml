[package]
name = "nlg-vocab-git"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Git / VCS activity vocabulary module for the nlg crate (commits, PRs, issues, reviews, releases)"

[dependencies]
nlg-core = { path = "../nlg-core" }

[dev-dependencies]
nlg-grammar-en = { path = "../nlg-grammar-en" }
