[package]
name = "prosaic-vocab-git"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Git / VCS activity vocabulary module for the prosaic crate (commits, PRs, issues, reviews, releases)"

[dependencies]
prosaic-core = { path = "../prosaic-core" }

[dev-dependencies]
prosaic-grammar-en = { path = "../prosaic-grammar-en" }
prosaic-grammar-es = { path = "../prosaic-grammar-es" }
